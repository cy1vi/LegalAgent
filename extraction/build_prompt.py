def build_prompt(category: str) -> str:
    """
    根据 dataset 项目的 案件类型 决定 prompt，专门用于提取影响判刑标准的关键词。
    """
    keywords_desc = """
    - 核心定性因素关键词 (core_factors): 用于直接决定适用罪名或法条的关键行为特征。示例：暴力威胁、秘密窃取、持械、入户、破坏防护装置、多人结伙、诈骗手段、非法占有目的等。
    - 从重处罚情节关键词 (aggravating_factors): 在核心定性确定后，对量刑起加重作用的情节。示例：累犯、犯罪集团、手段残忍、数额巨大、影响恶劣、危害公共安全等。
    - 从轻处罚情节关键词 (mitigating_factors): 在核心定性确定后，对量刑起减轻或从宽作用的情节。示例：初犯、认罪认罚、自首、退赃退赔、谅解、立功表现等。
    """


    if category == "侵犯财产罪":
        prompt = f"""
你是一个专业的法律案件分析助手，专门用于识别和提取侵犯财产罪案件中影响判刑标准的关键词。

案件特点：
- 涉及盗窃、诈骗、抢劫、挪用资金等侵犯他人财产权的行为
- 重点关注涉案金额、财物类型、损失情况等财务信息

请从提供的法律文书中提取以下三类关键词：

{keywords_desc}

输出要求：
1. 严格按照以下JSON格式输出结果：
{{
    "core_factors": ["找到的核心定性因素关键词"],
    "aggravating_factors": ["找到的从重处罚情节关键词"],
    "mitigating_factors": ["找到的从轻处罚情节关键词"],
    "analyze": "对fact中进行量刑情节的简要总结"
}}

2. 只输出JSON结果，不要添加其他解释文字
3. 如果某类关键词未找到，该类别应为空数组[]
4. analyze字段应包含对文中量刑情节的简要总结
"""
    
    elif category == "侵犯人身权利罪":
        prompt = f"""
你是一个专业的法律案件分析助手，专门用于识别和提取侵犯人身权利罪案件中影响判刑标准的关键词。

案件特点：
- 涉及故意伤害、杀人、强奸、绑架等侵犯他人人身安全的行为
- 关注受害人的伤情程度、伤亡情况
- 重视作案手段和危害后果

请从提供的法律文书中提取以下三类关键词：

{keywords_desc}

输出要求：
1. 严格按照以下JSON格式输出结果：
{{
    "core_factors": ["找到的核心定性因素关键词"],
    "aggravating_factors": ["找到的从重处罚情节关键词"],
    "mitigating_factors": ["找到的从轻处罚情节关键词"],
    "analyze": "对fact中进行量刑情节的简要总结"
}}

2. 只输出JSON结果，不要添加其他解释文字
3. 如果某类关键词未找到，该类别应为空数组[]
4. summary字段应包含对文中量刑情节的简要总结，特别关注伤害程度和作案手段
"""
    
    elif category == "经济犯罪":
        prompt = f"""
你是一个专业的法律案件分析助手，专门用于识别和提取经济犯罪案件中影响判刑标准的关键词。

案件特点：
- 涉及金融诈骗、非法集资、洗钱、逃税等破坏经济秩序的行为
- 通常涉及复杂的资金流向和财务数据
- 涉案金额较大，社会影响广泛

请从提供的法律文书中提取以下三类关键词：

{keywords_desc}

输出要求：
1. 严格按照以下JSON格式输出结果：
{{
    "core_factors": ["找到的核心定性因素关键词"],
    "aggravating_factors": ["找到的从重处罚情节关键词"],
    "mitigating_factors": ["找到的从轻处罚情节关键词"],
    "analyze": "对fact中进行量刑情节的简要总结"
}}

2. 只输出JSON结果，不要添加其他解释文字
3. 如果某类关键词未找到，该类别应为空数组[]
4. analyze字段应包含对文中量刑情节的简要总结，特别关注涉案金额和社会影响
"""
    
    elif category == "公共秩序犯罪":
        prompt = f"""
你是一个专业的法律案件分析助手，专门用于识别和提取扰乱公共秩序犯罪案件中影响判刑标准的关键词。

案件特点：
- 涉及妨害公务、聚众扰乱、寻衅滋事等破坏社会管理秩序的行为
- 通常具有群体性和社会影响性
- 重视行为对公共秩序的实际影响

请从提供的法律文书中提取以下三类关键词：

{keywords_desc}

输出要求：
1. 严格按照以下JSON格式输出结果：
{{
    "core_factors": ["找到的核心定性因素关键词"],
    "aggravating_factors": ["找到的从重处罚情节关键词"],
    "mitigating_factors": ["找到的从轻处罚情节关键词"],
    "analyze": "对fact中进行量刑情节的简要总结"
}}

2. 只输出JSON结果，不要添加其他解释文字
3. 如果某类关键词未找到，该类别应为空数组[]
4. analyze字段应包含对文中量刑情节的简要总结，特别关注社会影响和群体性特征
"""

    else:
        prompt = f"""
你是一个专业的法律案件分析助手，专门用于识别和提取影响判刑标准的关键词。

请从提供的法律文书中提取以下三类关键词：

{keywords_desc}

输出要求：
1. 严格按照以下JSON格式输出结果：
{{
    "core_factors": ["找到的核心定性因素关键词"],
    "aggravating_factors": ["找到的从重处罚情节关键词"],
    "mitigating_factors": ["找到的从轻处罚情节关键词"],
    "analyze": "对fact中进行量刑情节的简要总结"
}}

2. 只输出JSON结果，不要添加其他解释文字
3. 如果某类关键词未找到，该类别应为空数组[]
4. analyze字段应包含对文中量刑情节的简要总结
"""
    
    return prompt